import React from 'react'
import {
    SectionContainer,
    SectionWrapper
} from '../../common'

const  Experience  = () => {
    return (
        <SectionContainer id={'experience'}>
            <SectionWrapper>
                Experience
            </SectionWrapper>
        </SectionContainer>
    )
}

export default Experience
